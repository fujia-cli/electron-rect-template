import React from 'react';
import { Button } from '@ui-puzzles/rect';

const Home = () => {
  return (
    <section style={{ padding: 24 }}>
      <Button label="我是按钮" btnType="primary" size="large" />
    </section>
  );
};

export default Home;
